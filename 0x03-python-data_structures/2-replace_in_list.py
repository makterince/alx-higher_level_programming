#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    n = len(my_list)
    [element for idx in my_list for idx in element]:
        if idx < 0 and idx > n:
            return my_list
        else:
            return my_list[idx].element
